import 'package:flutter/material.dart';

abstract class AppTextStyles {
  static const littleWeek = TextStyle(fontSize: 18, color: Colors.grey);
  static const bigWeek =
      TextStyle(fontSize: 24, color: Colors.black, fontWeight: FontWeight.w700);
}

abstract class BottomNavBarOpt {
  static const rang = Colors.red;
  static const kattalik = 40;
}
